[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "xkglasses"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []
[project.scripts]
xkglasses = "xkglasses.cli:main"
[tool.setuptools.packages.find]
